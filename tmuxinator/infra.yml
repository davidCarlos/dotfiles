# ~/.tmuxinator/infra.yml

name: infra
root: /home/david/projects/Go/src/core

windows:
  - git:
        layout: even-horizontal
        panes:
         - gvim
         -
  - ejserver:
        panes:
         -
  - aws:
        panes:
         - ssh -i "etc/besouro/besouro_aws.pem" admin@ec2-18-222-20-172.us-east-2.compute.amazonaws.com
