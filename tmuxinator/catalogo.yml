# ~/.tmuxinator/catalogo.yml

name: catalogo
root: ~/projects/pencillabs/catalogo/catalogo-das-artes/

windows:
  - git:
        root: ~/projects/pencillabs/catalogo/catalogo-das-artes/
        layout: main-vertical
        panes:
         - vim
  - panes:
        root: ~/projects/pencillabs/catalogo/catalogo-das-artes/
        layout: tiled
        panes:
         - sudo systemctl stop nginx && sudo php -S 127.0.0.1:80
         -
