# ~/.tmuxinator/ej.yml

name: ej

windows:
  - git:
        root: ~/projects/pencillabs/ejplatform/ej-server
        layout: main-vertical
        panes:
         - gvim
         - htop
         -
  - panes:
        root: ~/projects/pencillabs/ejplatform/ej-server
        layout: tiled
        panes:
         - (cd docker && docker-compose up)
         -
         -
         -
