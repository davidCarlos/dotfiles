# ~/.tmuxinator/ej.yml

name: ej

windows:
  - git:
        root: ~/projects/pencillabs/ejplatform/ej-server
        layout: main-vertical
        panes:
         - sudo systemctl start docker && sudo chmod 777 /var/run/docker.sock && (cd docker && docker-compose up) 
         - htop
  - panes:
        root: ~/projects/pencillabs/ejplatform/ej-server
        layout: tiled
        panes:
         -
         -
