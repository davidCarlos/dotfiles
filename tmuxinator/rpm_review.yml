# ~/.tmuxinator/rpm_review.yml

name: package
windows:
  - pack:
        root: ~/rpmbuild
        layout: main-vertical
        panes:
          - (cd REVISIONS && vim)
          - cd REVISIONS
          -
