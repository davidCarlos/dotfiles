# ~/.tmuxinator/default_env.yml

name: web
root: ~/

windows:
  - MAIL:
      - mutt
  - IRC:
      - ssh  david@104.131.114.132  -t 'tmux attach'
  - auxiliar:
      - tmuxinator start dev_env
    

